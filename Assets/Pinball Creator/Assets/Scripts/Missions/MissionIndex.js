// MissionIndex.js : Description : use to return the mission index
#pragma strict

var mission_Index		: int = 0;					// Needed by the GameObject : "Manager_Game" on the Hierachy. <Important>  Must be different for each mission. 

function F_index () {
	return mission_Index;
}